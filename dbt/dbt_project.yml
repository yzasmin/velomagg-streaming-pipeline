name: velomagg
version: "0.1.0"
config-version: 2
profile: velomagg

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target"]

vars:
  fuseau: "Europe/Paris"

models:
  velomagg:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
